
%h2 Your cart
- @cart.each do |item|
  %p
  = "Item name: #{item.product.name} - Quantity: #{item.quantity} - "
  = link_to 'delete', shopping_cart_delete_path(item.product), { method: 'post' }

  %p
  = "Total: #{calculate_total}"
