
%h2 Your cart
%table.table.table-striped
  %th Item name
  %th Quantity
  %th Actions
  - @cart.each do |item|
    %tr
      %td= "#{item.product.name}"
      %td= "#{item.quantity}"
      %td= button_to 'delete', shopping_cart_delete_path(item.product), { method: 'post', class: 'btn btn-sm btn-danger' }

%p
= "Total: #{calculate_total}"
